{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug current file (libs too)",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      // "cwd": "${workspaceFolder}",
      // "env": {
      //   "PYTHONPATH": "${workspaceFolder}"
      // },
      "justMyCode": false
    }
  ]
}
